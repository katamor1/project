// .copilot/schemas/entry-request-packet.schema.json
// Defines the normalized Claude Code entry request packet schema used before session scope and dispatch checks.
// This exists so chat phrasing can be stabilized before the bridge calls the external runtime.
// RELEVANT FILES: .copilot/prompts/C1_intent_packet_builder.md, .copilot/schemas/entry-dispatch-packet.schema.json, docs/claude-code/runtime-bridge/dispatch-contract.md
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "entry-request-packet.schema.json",
  "title": "Entry Request Packet",
  "type": "object",
  "additionalProperties": false,
  "required": ["request_id", "user_intent", "requested_outcome", "requested_lane", "locale", "session_context_ref"],
  "properties": {
    "request_id": { "type": "string" },
    "user_intent": { "type": "string" },
    "requested_outcome": { "type": "string" },
    "requested_lane": { "type": "string" },
    "locale": { "type": "string" },
    "session_context_ref": { "type": "string" }
  }
}
